<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zjianhao.album">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name="com.SmartAlbumWithCloud.album.AppContext"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.SmartAlbumWithCloud.album.MainActivity"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.SmartAlbumWithCloud.ui.AlbumDetailAty" />
        <activity
            android:name="com.SmartAlbumWithCloud.ui.PhotoDetailAty"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" />
        <activity
            android:name="com.SmartAlbumWithCloud.ui.AdvancedSearchAty"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name="com.SmartAlbumWithCloud.ui.AmbigiousSearchAty" />
        <activity android:name="com.SmartAlbumWithCloud.ui.PhotoInfoAty" />
        <activity
            android:name="com.SmartAlbumWithCloud.ui.RegistAty"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name="com.SmartAlbumWithCloud.ui.LoginAty" />
        <activity
            android:name="me.nereo.multi_image_selector.MultiImageSelectorActivity"
            android:configChanges="orientation|screenSize" />

        <service android:name="com.SmartAlbumWithCloud.service.UploadService" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="com.SmartAlbumWithCloud.album.AlbumMapActivity"
            android:label="@string/title_activity_album_map"></activity>
    </application>

</manifest>